<%= form_with(model: mone) do |form| %>
  <% if mone.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mone.errors.count, "error") %> prohibited this mone from being saved:</h2>

      <ul>
        <% mone.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
